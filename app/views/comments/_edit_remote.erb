<%= form_for @editcomment,:url => {:action => "update", :controller => "comments"} do |f| %>
  <% if @editcomment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@editcomment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @editcomment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
