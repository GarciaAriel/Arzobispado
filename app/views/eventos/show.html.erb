

<form class="form-horizontal">
  <fieldset>

    <div class="form-group">
      <h2>
      		Titulo: <%= @evento.nombre%>
      </h2>
      <div class="panel panel-info">
        <div class="panel-heading">
          <i>
            <h4 class="panel-title">
          	  <%= @evento.descripcion%>
            </h4>
          </i>	
        </div>
      </div>
    </div>
    <div>
      	<h4>
	      Fecha inicio del evento: <%= @evento.fecha_inicio%> Fin del evento <%= @evento.fecha_fin%>
	    </h4>
    </div>

    
<div class="list-group">
  <%= link_to image_tag("ic_action_previous_item.png"), eventos_path ,:class=>"btn btn-primary"%>


  <% if current_user != nil && current_user.rol == "admin"%>  
    <%= link_to image_tag("ic_action_edit.png"), edit_evento_path(@evento),:class=>"btn btn-primary"%> |
  <%end%>  
  
    <%= link_to 'Cuestionarios', surveys_index_path(@evento.id) %>
</div>

	<div class="form-group">

    
	
	</div>

  </fieldset>
</form>
