<%= form_for(@survey) do |f| %>
  <% if @survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey.errors.count, "error") %> prohibited this survey from being saved:</h2>

      <ul>
      <% @survey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <%= f.fields_for :questions do |builder| %>
    <%= render 'question_fields', :ff => builder %>
  
  <% end %>
  <br>
  <div class="actions">
    <input type="button" onClick="elga(this)" value="Add Pregunta" > </button>
  </div>
  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<SCRIPT type="text/javascript">

  function elga(link) {
    alert("holaaaaaaaaaa");
    $(link).prev("input[type=hidden]").first().val("1");
    try{ //jQuery(this).parents("ul").find("h2").html("okay!")
      $(link).parent("div").find("ul").toggle('slow');
      alert("holaaaaaaaaaa");
    }
    catch(err)
    {
      alert(err.message);
    }
    return false;
  }
</SCRIPT>